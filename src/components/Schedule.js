import React from 'react';

const Schedule = () => {
    return (
        <div className="schedule">
            <h2>Schedule</h2>
            <input type="text" placeholder="Add Class..." />
            <button>Add Schedule Item</button>
        </div>
    );
};

export default Schedule;
